import socket

class AMQPSettings (object):
    port
    hostname
    queue
    def __init__ (port=5672, hostname=None, queue="default"):
        self.port = port
        self.hostname = hostname
        self.queue = queue
