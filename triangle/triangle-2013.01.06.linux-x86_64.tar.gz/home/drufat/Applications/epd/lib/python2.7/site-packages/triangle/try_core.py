import numpy as np
from triangle.core import *

a = np.array

pts = [[0, 0], [0, 1], [1, 1], [1, 0]]
pts = a(pts, dtype='double')
marks = [0, 0, 0, 0]
marks = a(marks, dtype='int32')
tri = TriangulateIO()
tri.pointlist = pts.flatten() 
tri.pointmarkerlist = marks
print a(tri.pointlist)
print tri.pointlist.size
print a(tri.pointmarkerlist)

in_ = tri
out_ = TriangulateIO()
triang('Vzq', in_, out_)

print a(out_.pointlist)
print a(out_.trianglelist)

print out_.numberofcorners